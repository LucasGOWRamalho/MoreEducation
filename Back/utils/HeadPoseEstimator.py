import numpy as np
import cv2

class HeadPoseEstimator:
    def estimate_pose(self, landmarks):
        # Placeholder: return dummy head direction vector
        return np.array([0, 0, 1])  # forward
